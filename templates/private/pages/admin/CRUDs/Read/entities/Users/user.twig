{% extends "private/pages/admin/CRUDs/Read/Read.html.twig" %}
{% block innerTitle %}{{ user.thename }} {{ user.thesurname }}{% endblock %}

{% block thead %}
    <tr>
        <th>Id</th>
        <th>Login</th>
        <th>Role</th>
        <th>Nom</th>
        <th>Prénom</th>
        <th>Email</th>
        <th>Status</th>
    </tr>
{% endblock %}
{% block tbody %}
    <tr>
        <td>{{ user.id }}</td>
        <td>{{ user.username }}</td>
        <td>
            {% for role in user.roles %}
                {{ role }} {% if not loop|last %}|{% endif %}
            {% endfor %}
        </td>
        <td>{{ user.thename }}</td>
        <td>{{ user.thesurname }}</td>
        <td>{{ user.themail }}</td>
        <td>{{ user.thestatus }}</td>
    </tr>
{% endblock %}