{% extends "layout/layout.html.twig" %}
{% block innerTitle %}Read User{% endblock %}
{% block main %}

    <table>
        <thead>
        <tr>
            <th>Id</th>
            <th>Login</th>
            <th>Nom</th>
            <th>Prénom</th>
            <th>Email</th>
        </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.id }}</td>
                <td><a href="{{ path("app_admin_read_user") }}/{{ user.username }}">{{ user.username }}</a></td>
                <td>{{ user.thename }}</td>
                <td>{{ user.thesurname }}</td>
                <td>{{ user.themail }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}